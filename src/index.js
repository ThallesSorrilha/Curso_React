import React from "react";
import ReactDOM from "react-dom/client";

// meus componentes
import HookForm from "./Hook_Form/HookForm6";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <HookForm />
  </React.StrictMode>
);
